# Print (guide)

*everything you need*
- approx charge `update min`
    - DONT overcharge (ruins battery)
- `red dot` = this side up
- `to save` :
    - `menu` + `select`
    - x
- `to load save` :
    - x

--- 

*important keybinds*
- `menu` + `select` opens RetroArch menu
- `menu` + `start` expands scale
- holding `R2` fast forwards
- `menu` + `R2` saves current state to slot