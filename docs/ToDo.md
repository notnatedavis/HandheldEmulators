# To Do :

- fix rg35xx+ (batteries swapped, need to reflash / format w/ win pc & rufus)
- fix miyoo issue w/ config and perma locking up
    - somehow updating the config file works but locks out after a second disabling ability to save state (v important)
    - I want the default config to be loaded with tweaked settings + ability to save states && always open retroarch
    - seems like its the issue with game & retroarch menu open (hotkey?i tried)
    - does the default work for an essentials only user ?
- disk health / partition analysis for miyoo (look for .iso online)